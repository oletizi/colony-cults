<!-- SPECKIT START -->
Active plan: specs/017-source-group-pdf/plan.md
For additional context about technologies to be used, project structure,
shell commands, and other important information, read the active plan above.
<!-- SPECKIT END -->

## Commandments

- **All UX/UI work MUST go through `/frontend-design:frontend-design`. NO EXCEPTIONS.** Never off-road and implement design work without it. Any task that creates or reshapes user-facing UI (layout, components, visual design, typography, styling) begins by invoking the frontend-design skill — before writing markup or CSS. This is non-negotiable and overrides any inclination to "just quickly" build UI directly.
- **Every query against an external online source MUST go through `/fetching-online-sources`. NO EXCEPTIONS.** Any query to a source repository or website whose content you will cite — discovery search, reconnaissance, metadata lookup, OCR/content read, or checking whether a source holds something — begins by invoking the fetching-online-sources skill, which mandates ONE sanctioned mechanism: the shipped `bib query-source` CLI client, which persists the raw page before returning, grounds every cited fact in the persisted bytes, paces and bounds the query, and gates exit-node escalation behind explicit operator approval (all enforced in code, constitution Principle XII). Never off-road with `curl` or shell HTTP, `WebFetch`, `WebSearch`-to-fetch-content, the raw `HttpClient`, or any ad-hoc/ungoverned browser call against a source URL — including driving the Playwright MCP browser directly for ANY source, registered or not — even for a "quick public GET", even when a source seems walled, even when a browser seems like overkill. There is NO fallback and no second channel: the client is the only path for EVERY source. An unregistered source is handled by registering a `SourceConfig` (the client persists the raw page even on a wrong selector, so you bootstrap the config from that capture) — never by hand-driving the Playwright MCP browser or any other tool. If the mechanism seems not to fit a case, fix the skill; never improvise a side channel.
- **Any process that retrieves an object, or writes an asset to the archive/object-store, MUST complete its SSOT bibliography metadata in the SAME operation. NO ORPHAN ASSETS. (Constitution Principle XV.)** When you acquire/mirror/retrieve a durable asset, the metadata update — assets, provenance, checksum, acquisition status, metadata snapshot — is NOT a follow-up step, a reminder, or a downstream reconcile you must remember to run: it is the first-class, unavoidable part of the operation, and the operation MUST fail loud rather than leave bytes in B2 or the archive that the SSOT record does not fully reflect. It is a DEFECT to finish an acquisition with the record left unadvanced (e.g. `status: to-collect` while masters sit in B2, or a missing `metadataSnapshot`). Design and test the metadata completion FIRST, not after. If a pipeline splits retrieval from the metadata write across steps you could forget (acquire now, remember to `reconcile` later), that split IS the bug — weld them so it is mechanically impossible to acquire without a complete record. The archive's value is its metadata; an object you cannot find through the record is not acquired, it is lost.
